# Vercel (serverless) dependencies. The local Streamlit version has its own
# requirements in legacy-streamlit/requirements.txt.
fastapi>=0.115
primp>=2.0           # Chrome-impersonating HTTP client (also a ddgs dependency)
beautifulsoup4>=4.12
google-genai>=1.51        # thinking_level (Gemini 3.x)
# DuckDuckGo search: the `duckduckgo-search` package was renamed to `ddgs`.
ddgs>=9.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0
pydantic>=2.7
