# Local development only. On Vercel, set these under
# Project → Settings → Environment Variables instead.
GEMINI_API_KEY=
SUPABASE_URL=
SUPABASE_KEY=
# Password the web page must send; the API refuses every request if unset.
APP_PASSWORD=
# Optional
# GEMINI_MODEL=gemini-2.5-flash
# GEMINI_MODE=auto
